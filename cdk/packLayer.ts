/**
 * Packs a layer with the exact dependency found in this project's package.json
 */
export const packLayer = async (
	packages: string[],
): Promise<{ layerZipFile: string }> => {
	return { layerZipFile: '' }
}
